import { expect } from 'chai';
import 'mocha';
import { ang } from './anglicize';

describe('ang', () => {
    it('runs', () => {
        console.log('PRINT THIS');
        const result = 1;
        expect(result).to.equal(1);
    });
});
